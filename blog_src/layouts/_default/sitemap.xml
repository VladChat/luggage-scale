{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">

  {{- $now := now }}

  <!-- Главная блога -->
  <url>
    <loc>{{ .Site.BaseURL }}</loc>
    <lastmod>{{ $now.Format "2006-01-02T15:04:05Z07:00" }}</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>

  {{- range .Site.RegularPages }}
    {{- /* Базовая дата публикации */ -}}
    {{- $pub := .Date -}}

    {{- /* lastmod для тега: берём более позднее из Date/Lastmod */ -}}
    {{- $lm := $pub -}}
    {{- if and .Lastmod (gt .Lastmod $pub) -}}
      {{- $lm = .Lastmod -}}
    {{- end -}}

    {{- /* Возраст считаем ИМЕННО от публикации, чтобы не влиял мнимый mtime */ -}}
    {{- $age := sub $now.Unix $pub.Unix -}}

    <url>
      <loc>{{ .Permalink }}</loc>
      <lastmod>{{ $lm.Format "2006-01-02T15:04:05Z07:00" }}</lastmod>

      {{- if lt $age 604800 }}           <!-- < 7 дней -->
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
      {{- else if lt $age 2592000 }}     <!-- 7–30 дней -->
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
      {{- else }}                        <!-- > 30 дней -->
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
      {{- end }}
    </url>
  {{- end }}

</urlset>
