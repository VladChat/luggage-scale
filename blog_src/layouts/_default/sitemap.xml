{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">

  {{- $now := now }}

  <!-- Главная блога -->
  <url>
    <loc>{{ .Site.BaseURL }}</loc>
    <lastmod>{{ $now.Format "2006-01-02T15:04:05Z07:00" }}</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>

  {{- range .Site.RegularPages }}
  {{- $refDate := .Date }}
  {{- if and .Lastmod (gt .Lastmod .Date) }}
    {{- $refDate = .Lastmod }}
  {{- end }}
  {{- $age := sub $now.Unix $refDate.Unix }}
  {{- if lt $age 0 }}{{ $age = 0 }}{{ end }}

  <url>
    <loc>{{ .Permalink }}</loc>
    <lastmod>{{ $refDate.Format "2006-01-02T15:04:05Z07:00" }}</lastmod>

    {{- if lt $age 604800 }}           <!-- < 7 дней -->
      <changefreq>daily</changefreq>
      <priority>0.9</priority>
    {{- else if lt $age 2592000 }}     <!-- 7–30 дней -->
      <changefreq>weekly</changefreq>
      <priority>0.7</priority>
    {{- else }}                        <!-- > 30 дней -->
      <changefreq>monthly</changefreq>
      <priority>0.5</priority>
    {{- end }}
  </url>
  {{- end }}

</urlset>
