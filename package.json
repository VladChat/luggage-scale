{
  "name": "luggage-scale",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ELEVENTY_ENV=dev npx @11ty/eleventy --serve --input=blog-src --output=blog-local",
    "build": "ELEVENTY_ENV=prod npx @11ty/eleventy --input=blog-src --output=blog",
    "dev:win": "set ELEVENTY_ENV=dev && npx @11ty/eleventy --serve --input=blog-src --output=blog-local",
    "build:win": "set ELEVENTY_ENV=prod && npx @11ty/eleventy --input=blog-src --output=blog",
    "generate:posts": "node scripts/generate-posts.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "3.1.2"
  },
  "dependencies": {
    "luxon": "^3.7.2"
  }
}
